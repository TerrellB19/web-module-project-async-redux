import React from "react";

const DogForm = (props) => {

    return (
        <div className="form">
            <form>
            <input />
            <button>Search</button>
            </form>
        </div>
        
    )
}

export default DogForm