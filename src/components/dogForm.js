import React from "react";

const DogForm = (props) => {

    return (
        <div className="form">
            <form>
            <button>Find Dog</button>
            </form>
        </div>
        
    )
}

export default DogForm